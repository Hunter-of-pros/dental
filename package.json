{
  "name": "dental-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run root-backend\"",
    "root-backend": "cd backend && node index.js",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
